# Config Schizophrenia

Detects when your configuration files have multiple personalities (different versions across environments). Run: `python config_schizophrenia.py` to diagnose inconsistent configs and environment variables that might break between local/staging/production.